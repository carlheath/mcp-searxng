# MCP SearXNG Configuration
# Copy to .env and fill in your values

# Required: URL to your SearXNG instance
SEARXNG_URL=https://searxng.ogmios.se

# Required: Authentication token for MCP API access
# Generate with: openssl rand -hex 32
MCP_AUTH_TOKEN=your-secure-token-here

# Optional: HTTP port (default: 3000)
MCP_HTTP_PORT=3000

# Optional: SearXNG authentication (if your instance requires it)
# AUTH_USERNAME=
# AUTH_PASSWORD=

# Optional: Custom User-Agent header
# USER_AGENT=MCP-SearXNG/1.0

# Optional: Proxy settings
# HTTP_PROXY=http://proxy:8080
# HTTPS_PROXY=http://proxy:8080
# NO_PROXY=localhost,127.0.0.1
